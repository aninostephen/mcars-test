import { Alert } from '@mui/material';
import React from 'react';

const ForOnHandUnit = ({ itemKey }) => {

    return (
        <div>
            
            <Alert severity="warning">Return this unit to on-hand.</Alert>
        </div>
    );
};

export default ForOnHandUnit;